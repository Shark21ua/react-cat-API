import { createActionTypes } from '../utils';

export const SIGN_IN = createActionTypes('SIGN_IN');

export const LOG_OUT = createActionTypes('LOG_OUT');

export const CHECK_AUTHORIZATION = createActionTypes('CHECK_AUTHORIZATION');
